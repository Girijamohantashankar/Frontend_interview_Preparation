q1. what is map()?

ans:- transforms array -> returns new array

const arr = [1,2,3,4];

const result = arr.map(x => x * 2);
[2,4,6,8]
const result = arr.map(x => x + 2);
[3,4,5,6]


q. whats is filter()?

const arr = [1,2,3,4];
const result = arr.filter( x > 2);
[3,4]

q3. what is reduce()?

const arr = [1,2,3];
const sum = arr.reduce((acc, curr) => acc + curr, 0);



practical questions:

task-1
sum of array arr=[10,20,30];

let arr = [10,20,30];

let result = arr.reduce((acc, curr) => acc + curr, 0);
console.log(result);


task-2
get even numbers

let arr = [1,2,3,4,5,6];

let even = [];

for(let i = 1; i < arr.length; i++){
    if(arr[i] % 2 === 0 ){
        even +=arr[i];
    }
};
console.log(even);

let even = arr.filter(a => a % 2 === 0);
console.log(even)


taks-3
 transform data 

const users = [
    {name: "A", age: 20},
    {name: "B", age: 30}
];
const names = users.map(u =>u.name);
console.log(names);



