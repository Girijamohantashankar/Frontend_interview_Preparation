var, let and const 



    let ---> block scope //global scope 

    example:-

    let a = 12;

    {
        let a = 20;
        console.log("This is block scope", a);
    }

    console.log("This is gobal scope", a);




    🚀 Practical Question 11 (React-Based)

Write a simple React component using useState

Task:

Create a counter with:

Increment button
Decrement button
Display current count



import React from 'react';
import { useState } from 'react'

function App() {
  
  const [count, setCount] = useState(0);

const handleIncrement = () =>{
  setCount(count + 1);
}
const handleDecrement = () =>{
  setCount  (count - 1);
}


  return (
   <>
     <p>{count}</p>
     <button onClick ={handleIncrement}>+</button>
     <button onClick={handleDecrement}>-</button>
     
   </>
  )
}

export default App




🚀 Practical Question 12 (React Props)

Create:

Parent Component → Child Component

Pass:

name = "Girija"
age = 25




import React from 'react';
import { useState } from 'react'
import Child from "./Child.jsx"

function App() {
  const title = "Frontend Developer"
  const company = "Google";


  return (
   <>
<Child title={title} company={company} />
     
   </>
  )
}

export default App



function child(props){
  <>
    <h1>name: {props.title}</h1>
    <h2>age: {props.company}</h2>
  </>
}











🚀 Practical Question 13 (Very Important)

Create a controlled input using useState

Task:
One input field
Whatever user types should show below

Example:

Input: [ hello ]

Output: hello


import React from 'react';
import { useState } from 'react'


function App() {
 const [input, setInput] = useState("");


  return (
   <>

<input placeholder="Enter the message" onChange = {(e) => setInput(e.target.value)} />
<p>{input}</p>
     
   </>
  )
}

export default App


🚀 Practical Question 14 (Very Important for Interviews)

Use useEffect to fetch API data

Task:

Fetch users from: