q. what is dependency array in useEffect ??

ANS-> Dependency array in useEffect like react  useEffect decides when the effect should be run , that means when any state or any varibale values was change then that effect or that particular functiona was run not all component was run .

useEffect(() => {
  // side effect code
}, [dependencies]);
