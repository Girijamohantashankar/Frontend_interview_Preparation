✅ 1. Question: Difference between var, let, const?

Ans:- These are all the type of variables declaration in the js . so in the var is function scope and other let and const is block scope.
var and let cab be reassign the value but var can redeclaer and let be redeclar and re assign the value , but const nither redeclar or reassign the value


var a = 10;
var a = 20; // allowed

let b = 10;
// let b = 20; ❌ error

const c = 10;
// c = 20; ❌ error



var--function scope, redeclar
let-block scope , re-assign and but do not redelar
const- block scope, neither re- declear, or re assign the value



✅ 2. Question: What is Hoisting?

ans:- hoisting is the js technic , declaraion variable are move to the top 

console.log(a); // undefined
var a = 5;


👉 Behind the scene:
var a;
console.log(a);
a = 5;


✅ 3. Question: Difference between == and ===?

ans:- == :- This operator are basically compaire the value 
      ===:- this operator are basically campaire the value and data type also 


"5" == 5   // true
"5" === 5  // false


✅ 4. Question: What is a Closure?

Closure is a basically machanism like functiona memeber variables are access to the outer function

function outer() {
  let count = 0;
  return function inner() {
    count++;
    console.log(count);
  };
}

const fn = outer();
fn(); // 1
fn(); // 2



✅ 5. Question: What is Event Loop?

ans:- event loop handels the async operations like seTimeout, setTimeInterval , call back , api callign etc



🎞PRACTICAL TASK

Task 1: Counter using Closure



function counter(){
    let count = 0;
    return function(){
        count++;
        return count;
    };
}
const c = counter();
console.log(c());
console.log(c());
console.log(c());




🧪 Task 2: Hoisting Test




console.log(a);
var a =10;


console.log(y);
let y = 20;




👉 Understand output:

var → undefined
let → ReferenceError



🧪 Task 3: Async Order

console.log("Start");
setTimeout(() =>{
    console.log("Timeout");
}, 0);

console.log("End");


👉 Output:

Start
End
Timeout














