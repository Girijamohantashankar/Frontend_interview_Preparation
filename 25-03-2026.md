✅ 1. Question: What is DOM?

Ans:- DOM is document object model is the structure of html where javascript is access and modify element

<h1 id="title">Hello</h1>


document.getElementById("title").innerText = "Hi";



✅ 2. Question: Difference between innerText, innerHTML, textContent?

Ans:-

innerText :- Vissible Text only

innerHtml :- Include HTML tags

textContext :- all text (hidden also )


element.innerText = "Hello";
element.innerHTML = "<b>Hello</b>";



✅ 3. Question: What is Event Bubbling?


ANS:-

Event goes from child to parent

document.getElementById("child").addEventListner("click", () => {
    console.log ("child");
})

document.getElementById("parent").addEventListener("click", () => {
  console.log("Parent");
});

Click child → Output:

Child
Parent


✅ 4. Question: What is Event Delegation?


ANS:- Using Parent to handels child events (better perfermance)

document.getElementById("list").addEventListener("click", (e) => {
  if (e.target.tagName === "LI") {
    console.log(e.target.innerText);
  }
});


✅ 5. Question: What is addEventListener?


ANS:- Used to attach events to elements.

button.addEventListener("click", () => {
  alert("Clicked");
});



🔹 DOM Selectors


document.getElementById()
document.querySelector()
document.querySelectorAll()

🔹 Event Types
Click
change
submit
input
keydown

🔹 Prevent Default

e.preventDefault();




💻 PRACTICAL TASK (VERY IMPORTANT)



🧪 Task 1: Button Click Counter

 
      <button id="btn">click me</button>
      
      
      <p id="count">0</p>


    let count = 0;

    document.getElementById("btn").addEventListener("click", () =>{
    count++;
    document.getElementById("count").innerText = count;
    });




🧪 Task 2: Add List Item Dynamically

 <input id="input" placeholder="enter the item name" />
      <button id="add">Add</button>
      
      <br>
      
      <ul id="list"></ul>


      
document.getElementById("add").addEventListener("click", () =>{
  
  const value = document.getElementById("input").value;
  
  const li = document.createElement("li");
  li.innerText = value;
  
  
  document.getElementById("list").appendChild(li);
  
});


🧪 Task 3: Event Delegation (Important for interviews)

document.getElementById("list").addEventListener("click", (e) => {
  if (e.target.tagName === "LI") {
    e.target.style.color = "red";
  }
});


1. Hoisting

console.log(a);
var a = 5;


ans:- undefiend

2. let vs var vs const

let- function scope , reassign;
var-- block scope, reassign , redeclear
const-- block scope , can not re-declear or re assign 



🧠 3. Closure

👉 Question:
What is closure?


A closure is when  a function members variables from its outer  scope even after the outer function is exceuted


✅ What is a Promise?

ANS:- A promise is an object that represent.

Pending
Resolved (success)
Rejected (error)

const promise = new Promise((resolve, reject) => {
  let success = true;

  if (success) {
    resolve("Success");
  } else {
    reject("Error");
  }
});

promise
  .then(res => console.log(res))
  .catch(err => console.log(err));





🔥 ASYNC / AWAIT (MODERN WAY)

👉 Cleaner way to handle promises

async function getData() {
  try {
    let res = await fetch("https://api.com/data");
    let data = await res.json();
    console.log(data);
  } catch (error) {
    console.log(error);
  }
}



❓ Q1: Difference between Promise and async/await?

👉 Answer:

Promise uses .then()
async/await is syntactic sugar over promises
async/await makes code more readable



❓ Q2: What is async function?
👉 Answer:
A function that always returns a Promise.


❓ Q3: What does await do?

👉 Answer:
It pauses execution until the Promise is resolved.



❓ Q4: Can we use await without async?

👉 Answer:
❌ No (only inside async function)




1.Loop pattern

for (let i =0; i < arr.length; i++)



2. Compare pattern

if(arr[i] > max)


3. String reverse pattern

str.split("").reverse.join("");
str.split("").reverse.join("");


4. set remove duplicate

[...new Set(arr)]

5. sum pattern

arr.reduce((a,b) => a+b, 0)


let arr = [1,1,2,3,3,4,5,6,6,7];
console.log([...new Set(arr)]);


PROBLEM 2 — FIND MAX NUMBER



let arr = [1,2,3,4,7,5,9,49];

let max = arr[0];

for(let i =1; i< arr.length; i++){
  if(arr[i] > max){
    max = arr[i];
  }
}

console.log(max);


PROBLEM 3 — REVERSE STRING

let reverseStr = "hello";

let result = reverseStr.split("").reverse().join("");
console.log(result);



PROBLEM 4 — PALINDROME



let palindrom = "madam";

let reverse = palindrom.split("").reverse().join("");

console.log(palindrom === reverse);


PROBLEM 5 — COUNT VOWELS (IMPORTANT)

let str = "this is the assesment rount test";
let count = 0;
let vowels = "aeiou";

for (let i =0; str < i.length; i++){
  if(vowels.includes(str[i])){
    count++;
  }
}

console.log(count);



Q1. let str = "frontend"

let result = str.split("").reverse().join("");
console.log(result);

Q2.  let num = [10, 25, 5, 40, 15];

let max = num[0];

for( let i = 1; i < num.length; i++){
  if(num[i] > max){
    max = num[i];
  }
}
console.log(max);


Q3. let arr = "level";

let reverse = arr.split("").reverse().join("");

console.log(arr === reverse);


Q4. let arr = [1,2,3,2,4,1,5];

let result = [...new Set(arr)];
console.log(result);

Q5.console.log( 2 + "2");
22