moneymul technical interview---->

job description--->


Job Description
We are looking for a creative and detail-oriented Frontend Engineer specializing in React.js to build modern, responsive web applications. Whether you are a collaborative Developer or a strategic Senior Developer, you will play a key role in defining the user experience. You will take full ownership of frontend modules from translating designs into code to ensuring the timely delivery of pixel-perfect interfaces.

Key Responsibilities:
Ownership & Delivery: Take end-to-end ownership of frontend features. Ensure code is delivered on time, meets quality standards, and matches the design specifications.
HLD: Define the frontend architecture (e.g., Micro-frontends, Component Library structure, State Management strategy).
LLD: Design reusable component hierarchies, define prop interfaces, and plan state flow (Redux/Context API) before coding.
React.js Development: Build highly responsive and scalable web applications using React.js (Functional Components, Hooks).
Performance Optimization: Ensure applications render fast by optimizing bundle sizes, implementing lazy loading, and minimizing re-renders.
API Integration: Seamlessly integrate RESTful API endpoints; handle loading states, error boundaries, and data caching efficiently.
Code Quality: Write clean, modular, and maintainable code. Conduct peer code reviews and implement unit testing (Jest/React Testing Library).


Requirements
Experience: 2-7+ years, strong execution skills; able to convert Figma/Adobe XD designs into code accurately, experience in architecture, optimizing complex applications, and mentoring juniors.
Frameworks: Deep expertise in React.js ecosystem (Router, Hooks, Context API).


Benefits
Impactful Work: Opportunity to contribute to projects used by over 100 top MNC companies and a large 10 million+ customer base.
Optimal Work Structure: We operate on a 5-day working schedule.
Professional Development: Committed to growth through eLearning, workshops, and the use of advanced AI models in day-to-day operations.
Comprehensive Well-being: Provision of on-demand comprehensive health insurance coverage with a Top-up facility for Parents, Spouse, and Children.




q. asking the do the trafic light system. like red , yellow, green one by one showing in delay 5 sec and when stop button click then red light on. again when start button click then similar one by one showing 



import React from 'react';
import { useState, useEffect } from 'react'
import "./styles.css";

function App() {
  const [light, setLight] = useState("red");
  const [running, setRunning] = useState(true);
  
  useEffect(() => {
    if(!running) return;
    
    const interval = setInterval(() => {
      setLight((prev) => {
        if(prev === "red") return "yellow";
         if(prev === "yellow") return "green";
          if(prev === "green") return "red";
      });
    }, 5000);
    return () => clearInterval(interval);
  }, [running]);
  
  const handelStop = () =>{
    setRunning(false);
    setLight("red");
  };
  
  const handelStart = () =>
  setRunning(true);
  
  
  
  
  const styles = {
    main: {
      padding: '20px',
    },
    title: {
      color: '#5C6AC4'
    },
    
    
    
  };

  return (
    <div style={styles.main}>
      <h1 style={styles.title}>Trafic light system</h1>
      <div className="trafic-light">
        <div className ={`light red ${light === "red"? "active" : "" }`} />
        <div className ={`light yellow ${light === "yellow"? "active" : "" }`} />
        <div className ={`light green ${light === "green"? "active" : "" }`} />
      </div>
      <div className="buttons">
        <button onClick = {handelStart}>Start</button>
        <button onClick = {handelStop}>Stop</button>
      </div>
      
    </div>
  )
}

export default App



.trafic-light{
  width: 100px;
  background: black;
  padding: 10px;
  border-radius: 8px;
  margin: auto;
}
.light{
  width: 60px;
  height: 60px;
  margin: 10px auto;
  border-radius: 50%;
  opacity: 0.3;
}

.red{
  background: red;
}
.yellow{
  background: yellow;
}
.green{
  background: green;
}
.active{
  opacity: 1;
}

button{
  margin: 10px;
  padding: 10px 20px;
  cursor: pointer;
}




p. Counter app

import React from 'react';
import { useState } from 'react'

function App() {
const [count, setCount] = useState(0);

  const handleIncrement=() =>{
    setCount(prev => prev + 1);
  }
  const handleDecrement = () =>{
    setCount(prev => prev -1);
  }


  return (
    <div>
      <h1>Increment and Decrement App</h1>
      <p>{count}</p>
      <button onClick={handleIncrement}>+</button>
      <button onClick={handleDecrement}>-</button>
     
    </div>
  )
}

export default App




p2.

import React from 'react';
import { useState } from 'react'

function App() {
const [count, setCount] = useState(0);

  const handleIncrement=() =>{
    if(count ===10){
      alert("You can not Increment more the 10")
      setCount(0);
    }else{
    setCount(prev => prev + 1);
    }
    
  }
  const handleDecrement = () =>{
    if(count === 0){
      alert("Negative value not assign")
    }else{
    setCount(prev => prev -1);
    }
  }


  return (
    <div>
      <h1>Increment and Decrement App</h1>
      <p>{count}</p>
      <button onClick={handleIncrement}>+</button>
      <button onClick={handleDecrement}>-</button>
     
    </div>
  )
}

export default App





import React from 'react';
import { useState, useEffect } from 'react'
import "./styles.css";

function App() {
  const [light, setLight] = useState("red");
  const [running, setRunning] = useState(true);
  
  
  useEffect( () =>{
    if(!running) return;
    
 const interval = setInterval(() => {
   setLight(prev =>{
     if(prev === "red") return "yellow";
     if(prev ==="yellow") return "green";
     if(prev === "green") return "red";
   });
 }, 1000);
return () => clearInterval(interval);
    
  }, [running]);
  
  const handleStart = () => {
    setRunning(true);
    
  }
  
  const handleStop = () => {
    setRunning(false);
    setLight("red");
  }


  return (
    <div>
      <h1>Trafic light system</h1>
      <div className="Trafic">
      <div className={`ligth red ${light === "red"? "active" : ""}`}></div>
      <div className={`ligth yellow ${light === "yellow"? "active" : ""}`}></div>
      <div className={`ligth green ${light === "green"? "active" : ""}`}></div>
      
     
  </div>
   <button onClick={handleStart}>Start</button>
      <button onClick={handleStop}>Stop</button>
     
    </div>
  )
}

export default App


.Trafic{
  background: black;
  border-radius: 10px;
  margin: 20px auto;
  width: 100px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.ligth{
  height: 40px;
  width: 40px;
  border-radius: 50%;
  opacity: 0.3;
  margin: 10px 10px;
}
.red{
  background: red;
}
.green{
  background: green;
}
.yellow{
  background: yellow;
}
.active{
  opacity: 1;
}

.btn{
  padding: 10px;
}

import React from 'react';
import { useState } from 'react'
import "./styles.css";

function App() {
  const [ison, setIson] = useState(false);
  
  const handleToggle = () =>{
    setIson(prev => !prev);
  }
  
  
  
  

  return (
    <div>
      <h1>Toggle Button On and Off</h1>
      <p>{ison ? "on" : "Off"}</p>
      
      <button onClick ={handleToggle} >Toggle</button>
 
     
    </div>
  )
}

export default App




p.

import React from 'react';
import { useState, useEffect } from 'react'
import "./styles.css";

function App() {
const [time, setTime]= useState(0);
const [running, setRunning] = useState(true);

const handleStart =() =>{
  setRunning(true);
}

const handleStop = () =>{
  setRunning(false);
}

useEffect (() =>{
  if(!running) return;
  
  const interval = setInterval(() => {
    setTime(prev => prev + 1);
  }, 1000);
  
  return () => clearInterval(interval);
  
}, [running])


  
  
  
  

  return (
    <div>
      <h1> Simple timer </h1>
      
      <p>{time}</p>
      
      <button onClick={handleStart}>Start</button>
      <button onClick={handleStop}>Stop</button>

 
     
    </div>
  )
}

export default App






import React from 'react';
import { useState, useEffect } from 'react'
import "./styles.css";

function App() {
const [input, setInput] = useState("");



  
  
  
  

  return (
    <div>
      <h1> Input with live preview </h1>
      <input placeholder="type your text" onChange = {(e) => setInput(e.target.value)} />
      
      <p>{input}</p>
      

 
     
    </div>
  )
}

export default App






